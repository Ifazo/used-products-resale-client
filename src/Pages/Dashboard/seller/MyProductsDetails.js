import React from 'react';

const MyProductsDetails = ({product, index}) => {
    const {name, price} = product;
    return (
        <div>
            
        </div>
    );
};

export default MyProductsDetails;